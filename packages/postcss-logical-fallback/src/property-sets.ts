export const inlineInsets = new Set([
  'inset-inline-start',
  'inset-inline-end',
  'inset-inline',
])

export const insets = new Set([
  'inset-inline-start',
  'inset-inline-end',
  'inset-inline',
  'inset-block',
  'inset-block-start',
  'inset-block-end',
])

export const borders = new Set([
  'border-start-start-radius',
  'border-start-end-radius',
  'border-end-start-radius',
  'border-end-end-radius',
])

export const float = new Set([
  'float',
  'clear'
])

export const floatValues = new Set([
  'inline-start',
  'inline-end'
])
